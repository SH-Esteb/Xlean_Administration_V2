local config = {}

config.Menu = "/amenu"

config.Tickets = "/tickets"

config.Staff = {
    ["superadmin"] = true,
    ["admin"] = true,
    ["modérateur"] = true,
    ["modérateur-test"] = true,
}

config.BanReason = {
    "Vous n'avez pas respecter les règles"
}

config.Logs = "/logs"

config.Warn = "/warn"

xlean_admin_config = config